# Retired project skills (2026-09-07)

Historical copies. Not loaded by any workflow (`core-agents.md` no longer lists them).

| Skill | Fate | Reason |
|---|---|---|
| `compress/` | merged into `caveman-compress` | `SKILL.md` identical (111 lines); single reference kept |
| `module-generator/` | archived, unused | Only self-reference in repo; no caller |
| `find-skills/` | deleted pin, use global | Repo copy stale vs global (`npx skills find --owner` missing); global skill covers it |

Rule: third-party/global skills (`huashu-design`, `supabase`,
`supabase-postgres-best-practices`, `caveman` family) are consumed as-is
and never edited here.
