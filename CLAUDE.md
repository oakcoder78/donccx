# CLAUDE.md

Pointer — the single agent contract lives in `AGENTS.md`. Read that file;
all routing rules are defined there (workflow: `module-detector` →
`docs-lookup` → `supabase-guard` → `change-classifier` → `docs-writer`).
